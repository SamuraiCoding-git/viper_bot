import React from 'react';
import '../index.css';  // Make sure this CSS is loaded

const Spinner = () => {
    return (
        <div className="loader"></div>  // Using the loader class from CSS
    );
};

export default Spinner;